info:
  name: Listar Lembretes
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/lembretes"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
