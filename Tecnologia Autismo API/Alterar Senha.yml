info:
  name: Alterar Senha
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/users/password"
  body:
    type: json
    data: |-
      {
        "currentPassword": "123456",
        "newPassword": "1234567"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
