info:
  name: Excluir Perfil
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/users/profile"
  body:
    type: json
    data: |-
      {
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
